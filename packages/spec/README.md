# APG Spec (v0.1 draft)

This package contains the open spec (schemas + examples) for **Agent Policy Gate**.
- `policy.yaml` — repo policy template
- `schemas/ops.plan.schema.json` — structured plan agents must produce
- `examples/` — passing/failing plans for conformance

License: CC-BY-4.0 for spec content.
